//jQuery


describe('webdriver.io page', () => {
    it('should have the right title', async () => {
        await browser.url('http://kendobuild-windows.dev.progress.com/kendo-ui/scheduler/index')
        await expect(browser).toHaveTitle('Demo of core features in jQuery Scheduler widget | Kendo UI for jQuery');
    })
})

describe('webdriver.io page', () => {
    it('should have the right title', async () => {
        await browser.url('http://kendobuild-windows.dev.progress.com/kendo-ui/grid/index')
        await expect(browser).toHaveTitle('Demo of core features in jQuery Grid widget | Kendo UI for jQuery');
    })
})

describe('webdriver.io page', () => {
    it('should have the right title', async () => {
        await browser.url('http://kendobuild-windows.dev.progress.com/kendo-ui/editor/index')
        await expect(browser).toHaveTitle('Demo of core features in jQuery Editor widget | Kendo UI for jQuery');
    })
})

describe('webdriver.io page', () => {
    it('should have the right title', async () => {
        await browser.url('http://kendobuild-windows.dev.progress.com/kendo-ui/window/index')
        await expect(browser).toHaveTitle('Demo of core features in jQuery Window widget | Kendo UI for jQuery');
    })
})

describe('webdriver.io page', () => {
    it('should have the right title', async () => {
        await browser.url('http://kendobuild-windows.dev.progress.com/kendo-ui/tooltip/index')
        await expect(browser).toHaveTitle('Demo of core features in jQuery Tooltip widget | Kendo UI for jQuery');
    })
})

describe('webdriver.io page', () => {
    it('should have the right title', async () => {
        await browser.url('http://kendobuild-windows.dev.progress.com/kendo-ui/dropdownlist/index')
        await expect(browser).toHaveTitle('Demo of core features in jQuery DropDownList widget | Kendo UI for jQuery');
    })
})

describe('webdriver.io page', () => {
    it('should have the right title', async () => {
        await browser.url('http://kendobuild-windows.dev.progress.com/kendo-ui/combobox/index')
        await expect(browser).toHaveTitle('Demo of core features in jQuery ComboBox widget | Kendo UI for jQuery');
    })
})

describe('webdriver.io page', () => {
    it('should have the right title', async () => {
        await browser.url('http://kendobuild-windows.dev.progress.com/kendo-ui/bar-charts/index')
        await expect(browser).toHaveTitle('Demo of core features in jQuery Bar Charts widget | Kendo UI for jQuery');
    })
})

describe('webdriver.io page', () => {
    it('should have the right title', async () => {
        await browser.url('http://kendobuild-windows.dev.progress.com/kendo-ui/daterangepicker/index')
        await expect(browser).toHaveTitle('Demo of core features in jQuery DateRangePicker widget | Kendo UI for jQuery');
    })
})

describe('webdriver.io page', () => {
    it('should have the right title', async () => {
        await browser.url('http://kendobuild-windows.dev.progress.com/kendo-ui/numerictextbox/index')
        await expect(browser).toHaveTitle('Demo of core features in jQuery NumericTextBox widget | Kendo UI for jQuery');
    })
})

