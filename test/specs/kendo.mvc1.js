// MVC

describe('webdriver.io page', () => {
    it('should have the right title', async () => {
        await browser.url('http://kendobuild-windows.dev.progress.com/aspnet-mvc/scheduler/index')
        await expect(browser).toHaveTitle('Demo for core features in ASP.NET MVC Scheduler control | Telerik UI for ASP.NET MVC');
    })
})

describe('webdriver.io page', () => {
    it('should have the right title', async () => {
        await browser.url('http://kendobuild-windows.dev.progress.com/aspnet-mvc/grid/index')
        await expect(browser).toHaveTitle('Demo for core features in ASP.NET MVC Grid control | Telerik UI for ASP.NET MVC');
    })
})

describe('webdriver.io page', () => {
    it('should have the right title', async () => {
        await browser.url('http://kendobuild-windows.dev.progress.com/aspnet-mvc/editor/index')
        await expect(browser).toHaveTitle('Demo for core features in ASP.NET MVC Editor control | Telerik UI for ASP.NET MVC');
    })
})

describe('webdriver.io page', () => {
    it('should have the right title', async () => {
        await browser.url('http://kendobuild-windows.dev.progress.com/aspnet-mvc/window/index')
        await expect(browser).toHaveTitle('Demo for core features in ASP.NET MVC Window control | Telerik UI for ASP.NET MVC');
    })
})

describe('webdriver.io page', () => {
    it('should have the right title', async () => {
        await browser.url('http://kendobuild-windows.dev.progress.com/aspnet-mvc/tooltip/index')
        await expect(browser).toHaveTitle('Demo for core features in ASP.NET MVC Tooltip control | Telerik UI for ASP.NET MVC');
    })
})

describe('webdriver.io page', () => {
    it('should have the right title', async () => {
        await browser.url('http://kendobuild-windows.dev.progress.com/aspnet-mvc/dropdownlist/index')
        await expect(browser).toHaveTitle('Demo for core features in ASP.NET MVC DropDownList control | Telerik UI for ASP.NET MVC');
    })
})

describe('webdriver.io page', () => {
    it('should have the right title', async () => {
        await browser.url('http://kendobuild-windows.dev.progress.com/aspnet-mvc/combobox/index')
        await expect(browser).toHaveTitle('Demo for core features in ASP.NET MVC ComboBox control | Telerik UI for ASP.NET MVC');
    })
})

describe('webdriver.io page', () => {
    it('should have the right title', async () => {
        await browser.url('http://kendobuild-windows.dev.progress.com/aspnet-mvc/bar-charts/index')
        await expect(browser).toHaveTitle('Demo for core features in ASP.NET MVC Bar Charts control | Telerik UI for ASP.NET MVC');
    })
})

describe('webdriver.io page', () => {
    it('should have the right title', async () => {
        await browser.url('http://kendobuild-windows.dev.progress.com/aspnet-mvc/daterangepicker/index')
        await expect(browser).toHaveTitle('Demo for core features in ASP.NET MVC DateRangePicker control | Telerik UI for ASP.NET MVC');
    })
})

describe('webdriver.io page', () => {
    it('should have the right title', async () => {
        await browser.url('http://kendobuild-windows.dev.progress.com/aspnet-mvc/numerictextbox/index')
        await expect(browser).toHaveTitle('Demo for core features in ASP.NET MVC NumericTextBox control | Telerik UI for ASP.NET MVC');
    })
})